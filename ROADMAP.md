# Roadmap

## Future candidates

Originally deferred from v0.1 by explicit decision (spec "Deferred to v0.2"
section + post-release distillation audit). They are not promises for a
specific version. Each entry names its trigger — build it when the trigger
fires, not before (`enforces: P6`).

| Item | What | Trigger to build |
|---|---|---|
| Conformance suite | Golden-fixture end-to-end tests: full lifecycle, resume, one degraded dispatch tier, on both harnesses | First contract regression that structural validation (`scripts/validate.sh`) fails to catch |
| Schema validators + fixtures | plan/v1 and review-envelope/v1 validators with valid/invalid/legacy/migration fixtures | First malformed plan or envelope produced in real use — **fired** (out-of-enum values tracked in a consuming repo, 2026-07-26); plan/v1 half closed by this cycle (`feat/plan-status-terminal-states`); review-envelope/v1 half remains open under the original trigger |
| Session-history search | Pluggable prior-context source for session-scoped retros (ce-sessions distillation) | First session retro that needed "what was tried before" and couldn't answer it |
| compound-refresh headless auto-apply | Apply unambiguous Keep/Update actions headlessly (v0.1 is recommend-only) | After ~3 manual refresh cycles show the classifications are trustworthy |
| Cross-round deepening suppression | Skip re-surfacing findings the user rejected in a prior deepening round (evidence-overlap keyed) | First complaint about repeat findings in interactive deepening |
| Demo/evidence capture | ce-demo-reel distillation: GIF/screenshot evidence for PR bodies of UI-observable changes | First UI-heavy project adopting `shipping` |
| Project-defined lane schema | Formal schema for custom review lanes (v0.1: free-form markdown in the consuming repo's AGENTS.md) | Second project defining custom lanes |
| Ambient compound triggers | "that worked / it's fixed" phrase detection auto-offering `compound` (v0.1: explicit calls + skill descriptions) | Evidence that solved problems routinely go uncaptured outside release-loop |
| Gemini support verification | `ask_user` question-tool path is documented but untested | First Gemini-harness user |
| Evidence-tier vocabulary | Shared CONCEPTS.md ladder of completion-evidence strength (failing-repro-now-passing > end-to-end run > integration test > unit test > typecheck/build; typecheck alone never closes a completion claim, a unit test only closes a unit-level claim) plus binary reporting language (`verified: <observation>` / `unverified: <blocker>`, no "should work" hedges) consumed by reviewing/shipping/retrospective | First completion claim that escapes review because it was verified at a lower layer than the requirement lived at |
| Skill-level trace evidence | Structured channel where a retro records which skill *section* the session confirmed, contradicted, or refined; compound-refresh consumes the rows as skill-maturity evidence (today lessons reach docs/solutions/ and ROADMAP, never a per-skill-section record) | First retro lesson that contradicts a shipped skill's guidance rather than the code under work |
| New-skill distinctness gate | Absorb-over-add rule for skill proposals: a candidate must show evidence distinct from every existing skill or be absorbed into the nearest one — complements trigger-to-build (which decides *when* to build; this decides *whether*) | Next new-skill proposal (14th skill) |

The last three rows come from the 2026-07-23 survey of
[rlaope/ultraprompt](https://github.com/rlaope/ultraprompt)
([docs/research/ultraprompt-survey.md](docs/research/ultraprompt-survey.md)) —
external idea imports, not v0.1 deferrals. The survey's fourth import
(hypothesis kill criteria / predict-before-probe / boring hypothesis) was applied
directly to `skills/debugging/SKILL.md` the same day and is not tracked here.

## Shipped

| Item | Release | Evidence | Follow-up boundary |
|---|---|---|---|
| `release` skill (13th): local post-merge CHANGELOG, synchronized manifests, first-hand release/publication gates, annotated tags, prepare-only headless handoff, and resumable outward publication | `v0.3.0` | Release commit `22b4d85`; tag `v0.3.0`; [GitHub release](https://github.com/teslamint/compound-loop/releases/tag/v0.3.0); first live smoke classified matching branch/tag, executed only `page-create`, then independently verified `noop/fully-matching` with a byte-exact CHANGELOG body | Local ceremony originally shipped at `v0.2.0` (`c3cbf01`); protected `v0.2.0` remains non-republishable through normal publication; future versions reuse the same separate-consent, missing-suffix, fail-closed boundary |

## Carry-forward from retros

Open items registered by retro docs (`docs/retros/`), pushed here per `retrospective` Phase 4 (durable tracker, never retro-doc-only). Remove a row when its retro reconciliation marks it Done.

| Item | Origin | Priority | Trigger / next step |
|---|---|---|---|
| Clean-environment Codex install check: plugin-native skill discovery (`.codex-plugin/plugin.json`) has never been isolated from the dev machine's `~/.agents/skills/` symlinks | 2026-07-16 v0.1 release retro | P3 | First external or clean-machine Codex install |
| ~~Automated numbered-reference validation for planning and plan schema: prove contiguous heading/list numbering and resolve planning-step references across `skills/planning/SKILL.md`, `skills/planning/references/*.md`, and `schemas/plan-schema.md`~~ **Done** numbered-reference-validation cycle | 2026-07-18 process-guidance carry-forward retro | P3 | ~~Before the next numbered planning-step or plan-schema hard-floor insertion, add the check to structural validation — **fired** 2026-07-24 (planning-trigger-audit cycle performed both insertions; deferred at planning with manual numbering acceptance checks carried in-plan; check still unbuilt)~~ fired: numbered-reference-validation cycle |
| Pin the tracked Python support contract in every non-fixture compatibility consumer, including the publication harness delegation | 2026-07-19 Python compatibility gate retro | P3 | Before the next publication-harness or compatibility-consumer edit, set `PYTHON_SUPPORT_FILE` explicitly at the delegation boundary |
| ~~Carry-forward check needs a structural assertion: the end-of-interview carry-forward check (probed-row→T-ID linkage) is prose-only and had zero execution evidence across all three dry runs (dry-run evidence doc erratum 4); the 2026-07-21 retro's commit records its first execution, but nothing mechanical fails a retro doc whose probed rows lack `(T<n>)` citations~~ **Done** carry-forward-tid-check cycle | 2026-07-21 retro-interview-enforcement retro (T5) | P3 | ~~First retro where a probed carry-forward row survives to commit without its T-ID citation, or the next retro-template/check-9 design cycle — **fired** latched (2026-07-24 planning-trigger-audit plan audit via the prior retro's strict-reading record; deferred, retro-template/check 9 out of that cycle's scope)~~ fired: carry-forward-tid-check cycle |
| ~~Plan internal clause-consistency check: planning self-review, independent plan review, and implementing preflight all validate plan-against-spec or across units — none diffs a plan's architecture-note prose against its unit step contracts, which let the F2 contradiction (deviation addendum 003) survive all three layers~~ **Done** plan-clause-consistency cycle | 2026-07-21 retro-interview-enforcement retro (T4) | P3 | ~~Next edit to `skills/planning/SKILL.md` self-review or `skills/implementing/SKILL.md` preflight, or the next plan whose architecture notes summarize unit behavior — **fired** third consecutive cycle (2026-07-24 planning-trigger-audit U3 self-review edit); satisfied procedurally again (plan-review clause-diff returned F3/F4, fixed `1df40a9`); mechanical check still unbuilt~~ fired: plan-clause-consistency cycle |
| Interview protocol vocabulary gaps (deferred by design, not oversight): `Previous doc shape` has no value for "no previous retro doc exists" (first retro); no closed verdict form for a dispatch cap exhausted mid-exchange (before 3 rejections); Round-span notation (`1→2`) and qualified/composite accepted-verdict forms are used by the dry runs but not exemplified in `schemas/retro-template.md`; `references/interview-probes.md` maps only `self-checklist` mode — the in-thread verdict form is stated only generically in SKILL.md | 2026-07-21 retro-interview-enforcement reviewing phase, pre-merge (F14, F15 + final-review minors U6-m2, U6-m4, U3-m2) | P3 | First real retro that hits one of these cases, or the next interview-protocol design cycle — vocabulary changes go through `designing`, not a patch |
| check 9 / drift-harness coverage follow-ups: check 9's malformation-guard branches (level count ≠ 4, verdict forms ≠ 3, missing `Verdict cell values:` line) and the harness's own cleanup/TMPDIR failure paths have no fixture cases; a hypothetical paren-leading verdict form would be silently skipped by the anchor extraction | 2026-07-21 retro-interview-enforcement reviewing phase, pre-merge (F11, F12 + final-review minor U5-m2) | P3 | Next edit to `scripts/validate.sh` check 9 or `scripts/test-retro-format-drift.sh` |
| ~~Pre-existing validate.sh check 5 raises a Python traceback on an unreadable `schemas/*.md` file instead of a named `FAIL:` line (loud-failure convention that checks 6 and 9 follow)~~ **Done** validate-robustness-batch cycle | 2026-07-21 retro-interview-enforcement reviewing phase, pre-merge (U5-m3, pre-existing/out of scope) | P4 | ~~Next validate.sh robustness pass, or first time check 5 tracebacks in real use~~ fired: validate-robustness-batch cycle (session added checks 12, 13 — robustness extension) |
| Release headless-path `.release/draft.md` carries no in-file non-authorization marker — the spec's "every persisted packet" invariant vs R7's interactive-only scope (U4-m1); resolve via `designing` (extend the marker to the headless write, or scope the invariant with an explicit exception) | 2026-07-22 final-action-session-resilience retro (T2) | P3 | Next design cycle touching release's draft contract or the marker invariant |
| ~~`final_action` record polish: add a marker/note slot to the schema block so the per-packet marker lives at data level and shipping's byte-for-byte single-sink claim is literally satisfiable (N-1); give `predicted → determined` the same explicit same-edit Log clause as the other transitions (N-2)~~ **Done** final-action-polish cycle | 2026-07-22 final-action-session-resilience retro (T3, T4) | P3 | ~~Next edit to `skills/release-loop/references/progress-schema.md` or release-loop Gate handling~~ fired: final-action-polish cycle |
| ~~Define "hand-up packet" locally in `skills/shipping/SKILL.md` (used at Steps 7–8 without definition; pre-existing, surfaced by U3 review)~~ **Done** shipping-skill-polish cycle | 2026-07-22 final-action-session-resilience retro (U3-m1) | P4 | ~~Next shipping SKILL edit — fired `2299955` (prior cycle), latched at 2026-07-24 planning-trigger-audit plan audit and deferred (shipping SKILL untouched that cycle)~~ fired: shipping-skill-polish cycle |
| ~~Mechanical `scripts/validate.sh` check for `final_action` shape (plan Deferred item)~~ **Done** final-action-validate cycle | 2026-07-22 final-action-session-resilience plan, Deferred to Follow-Up Work | P3 | ~~First observed drift in a real progress.md `final_action` record — **fired** (recurring out-of-schema `note:` field, second consecutive cycle, per 2026-07-24 retro T1); latched at 2026-07-24 planning-trigger-audit plan audit, deferred (validate.sh out of scope); check still unbuilt~~ fired: final-action-validate cycle |
| ~~Vocabulary polish batch: add "claim layer" to `skills/reviewing/SKILL.md:90`'s parenthetical (U2-m1) and rephrase `skills/shipping/references/verification.md:37`'s "it names the tier cited" subject (U3-m1)~~ **Done** vocab-polish-batch cycle | 2026-07-24 evidence-tier-vocabulary retro (T5) | P4 | ~~Next cycle that edits either file~~ fired: vocab-polish-batch cycle |
| ~~Spec-level carve-out rule: when a spec pairs a universal principle with a requirement mandating an apparent exception, the spec itself names the carve-out — the R5-vs-Architecture tension reached planning unresolved and was caught only by the plan review's mandated clause-diff~~ **Done** spec-carveout-rule cycle | 2026-07-24 evidence-tier-vocabulary retro (T3) | P3 | ~~Next `designing` cycle whose spec states a universal principle alongside per-requirement mechanisms — **fired** 2026-07-24 (planning-trigger-audit spec's gloss-boundary vs R2 pairing); satisfied procedurally in-spec (explicit carve-out sentence); durable `designing` rule still unbuilt~~ fired: spec-carveout-rule cycle |
| ~~Reword plan-schema audit-section provenance: "approved with this plan" cannot be resolved by a future reader — name the plan path (`docs/plans/2026-07-24-001-feat-planning-trigger-audit-plan.md`)~~ **Done** plan-status-terminal-states cycle | 2026-07-24 planning-trigger-audit retro (U2-m1, final-branch-review CARRY) | P4 | ~~Next `schemas/plan-schema.md` edit~~ fired: plan-status-terminal-states cycle (U1 `f33ba6b`) |
| ~~Fix stale "item 1's deviation-addendum rule" numeric references at `schemas/plan-schema.md:32` and `skills/planning/SKILL.md:120` — the deviation-addendum rule lives in hard-floor item 3 (Assumption Recheck), pre-existing defect~~ **Done** plan-status-terminal-states cycle | 2026-07-24 planning-trigger-audit plan review (F4 observation) | P4 | ~~Next edit to either file~~ fired: plan-status-terminal-states cycle (U1 `f33ba6b`, U4) |
| Retro-side trigger classification rule: reconciliation classifies each carry-forward row's trigger class (edit/drift/event-based) before classifying its status — registers the planning-trigger-audit spec's Open Decision (owner: retro) | 2026-07-24 planning-trigger-audit spec, Open Decisions | P3 | Next `retrospective`-skill design cycle, or first reconciliation that misclassifies a row status on a softened trigger reading |
| `skills/compound/scripts/validate-frontmatter.py` is expected to run on CPython 3.8, but 3.8 is not a boundary interpreter — the compatibility gate compiles the file only on 3.9 and 3.14, so a later edit can reintroduce a 3.9-only construct and silently break 3.8 with every check still green. Incidental compatibility with no mechanical guard; the guard would require moving `minimum_minor`, which the user declined at the Design gate | 2026-07-26 frontmatter-validator-python38 spec (R5) | P3 | First edit **after 2026-07-26** to `skills/compound/scripts/validate-frontmatter.py` or to `schemas/python-support.json` — the cycle that registered this row edited the validator itself, and that commit does not fire it |
| ~~Two pre-existing red suites on `main`, unowned: `scripts/test-signal-drift.sh` Case D asserts a span on `skills/retrospective/SKILL.md:77` that the line no longer contains (it is blank; Case D target confirmed still at line 94 after U5's edit, asserted by U5 step 2 — repair value = 94), and `scripts/test-release-publication.sh` `local_release_regression` fails its byte comparison against the `48eccb0` base (99 passed / 1 failed). Both reproduce identically at `60df670` and at `adb310d`, so neither is a regression from any recent cycle — they are decayed fixtures nobody has claimed~~ **Done** fix-red-suites cycle | 2026-07-26 frontmatter-validator-python38 retro | P2 | ~~Next edit to either harness, or to `skills/retrospective/SKILL.md`; whichever comes first~~ fired: fix-red-suites cycle |
| ~~Command-closure check for plan steps: every command in a step must define or explicitly declare its inputs and create its prerequisites before use, verifiable by running the step in a clean shell with `set -u`. The existing no-placeholder scan is keyword-based (TBD / TODO / "as appropriate") and structurally cannot catch a step that is prose-complete but dataflow-incomplete — `$BASELINE` was referenced and never assigned, surviving both plan self-review and an independent plan review~~ **Done** command-closure-check cycle | 2026-07-26 frontmatter-validator-python38 retro (PR #1 comment 3652230761) | P3 | ~~Next edit to `skills/planning/SKILL.md` step 13 or step 14, or to `schemas/plan-schema.md`'s unit template~~ fired: command-closure-check cycle (step 14 edited by plan-clause-consistency cycle) |
| `scripts/test-python-compatibility.sh` runs every parsing and fixture heredoc on `BOOTSTRAP="${PYTHON_BOOTSTRAP:-python3}"` — the ambient interpreter, resolved before any boundary interpreter provides protection. It passes today under `PYTHON_BOOTSTRAP=python3.8`, but nothing declares or gates that, so a 3.9-only construct in any heredoc would break `bash scripts/validate.sh` outright on a 3.8-ambient machine. Incidental compatibility again, one layer below the row above; ranked lower because the failure is loud rather than silently green | 2026-07-26 frontmatter-validator-python38 retro (facilitator-originated, T9) | P4 | Next edit to `scripts/test-python-compatibility.sh` that adds or changes an inline Python heredoc |
| ~~Shipping commit-message regeneration after review: a message drafted before review is stale after review changes the artifact~~ **Done** shipping-skill-polish cycle | 2026-07-27 plan-status-terminal-states cycle | P3 | ~~Next `skills/shipping/SKILL.md` edit or first stale-message incident~~ fired: shipping-skill-polish cycle |
| ~~`source-over-memory` cross-cutting reference: claims about file content require a same-turn read~~ **Done** source-over-memory cycle | 2026-07-27 plan-status-terminal-states cycle | P3 | ~~Next memory-based citation error, or next cycle touching citation rules in planning/shipping/retrospective/compound~~ fired: source-over-memory cycle (cross-cutting rule added to AGENTS.md Global Agent Rules) |
| Reserved designing cycle: post-approval body immutability (every section, every actor: an approved plan's §-gate was edited in place three times and the approved record erased, restorable only via byte-exact revert plus a deviation addendum) + outward publication named as a stateful ceremony (a deliverable created a public repository, pushed, force-pushed twice, and switched visibility with no mutation/failure-state matrix, because nothing named outward publication as one) | 2026-07-27 plan-status-terminal-states cycle | P2 | Before the next cycle whose deliverable crosses an outward-publication boundary, or first post-approval in-place edit in any plan/v1 repo |
| `execution: ops` as a possible third execution mode: two instances tracked in a consuming plan/v1 repo against an enum of `code \| non-code` | 2026-07-27 plan-status-terminal-states cycle | P3 | First `execution: ops` instance in this repo, or the first request to adopt the validator in a consuming repo |
| ~~Fix `Plan:` → `plan:` casing at `skills/planning/SKILL.md:176` to match `progress-schema.md:19` — latent defect activated by the done-flip reader; pre-existing at `d75210e` (2026-07-16)~~ **Done** `da1fdf9` | 2026-07-27 plan-status-terminal-states retro (T4) | P3 | ~~Next `skills/planning/SKILL.md` edit~~ fired: plan-skip-documentation cycle |
| ~~validate.sh check 11 skip-path fixture test: the no-progress.md conditional skip was verified by code inspection only (T2); a fixture case (run validate.sh in a copy with no `.release-loop/`) would mechanically cover it~~ **Done** validate-robustness-batch cycle | 2026-07-29 final-action-validate retro (T2) | P4 | ~~Next edit to `scripts/validate.sh` check 11 or next fixture-test expansion cycle~~ fired: validate-robustness-batch cycle (session added checks 12, 13 — fixture expansion) |

## Non-goals (re-affirmed)

- Porting the remaining product-specific compound-engineering skills (dhh-rails-style, gemini-imagegen, riffrec, proof, promote, product-pulse, test-xcode, slack-research, polish, dogfood-beta, strategy).
- ce-optimize's full experiment loop — its measurable-goal discipline already lives in `designing`'s required Success Criteria + `retrospective`'s measured-vs-declared pass.
