## Scope

- reviewer_identity: `Claude Code independent invariant-attack reviewer`
- reviewer_session: `InvariantAttackU5`
- reviewer_model: `anthropic/claude-fable-5`
- orchestrator_session: `Main`
- reviewed_head: `4e6d45030c42ee07080b8eddc71d86e0516502c0`
- full_diff_sha256: `eb80ac5f49a27b32d052a6befaaf6c417129a88fa40602784c9aaf2f12622d06`
- approved_spec_sha256: `46f0407fa3982747576e71b16ce6ee7a3e6560b17e62df22417cb522e1a4185a`
- approved_plan_sha256: `58f378aa99014b4bc7daca0f8ebc651486843fb78ad9cce2eed7de46e9819929`
- addendum_sha256: `ee95a384314650f616190df931af5a50c4c91ab22af0ff9df778f7df4259adb9`
- u3_u4_evidence_manifest_sha256: `5c792f5b332dcc5e864a748a4039ab137ef07380bd40da2d35ef5b700cc3291f`

This was a fresh read-only attack on the final U1–U4 branch, not a conformance pass over the approved plan. I reviewed the net 14-file branch diff from merge base `86586a0e2569212400df00350912d8a7b4199978`, the shipped check and lifecycle wording, the approved spec, sealed plan, addendum 012, the three U3 outward-action artifacts, and every canonical U3/U4 evidence record. The manifest contains 48 records: six outcomes for each of T0 through T7. Every recorded path, byte size, and SHA-256 matched the current file; the canonical compact-record-list digest matched the value above; the approved plan body matched its `body_seal`; and the manifest's reviewed head and full-diff digest matched current Git bytes.

## Carry-forward re-derivation

The final net file list is the 14 paths in the reviewed diff: the addendum; three issue-closure artifacts; the approved plan; four persisted spec/plan review artifacts; the approved spec; `skills/planning/SKILL.md`; `skills/shipping/SKILL.md`; `skills/release-loop/SKILL.md`; and `skills/release-loop/references/progress-schema.md`. Runtime evidence under `.release-loop/evidence/U3/` and `.release-loop/evidence/U4/` is disposable loop state and was audited separately through the canonical 48-record manifest.

Re-reading `ROADMAP.md` rather than copying the plan table produced 12 open Future candidates and 9 open carry-forward rows, 21 open rows total. The final fired set is nine rows: Conformance suite and Schema validators + fixtures remain latched; Shipping can delete the worktree owning live loop state, Forced-failure matrices omit partial durable state, Loop artifacts outliving their loop, invariant-attack review, criterion-based severity, and the human-discharged completion gate fire from the final file/event set; and the dispatched-commit signature row now fires because U1–U4 were committed by dispatched implementers. The four implementation commits all report `%G? = G`, so that last row's cycle-local control was discharged. The post-Retro criterion row and reviewer-persistence row do not fire by their written triggers, although reviewer persistence is voluntarily discharged here. The remaining Future-candidate triggers do not fire. Result: 21 open, 9 fired, 0 unobservable, with every fired row folded, deferred with a named reason, or discharged by its specified conditional control.

## SC2 invariant attack

- **Reject A — different artifact kinds.** Observed result: `rejected`. A `.mlmodelc` digest compared with an `.mlpackage` digest cannot pass. Deciding clause: `Different artifact kinds in the real comparands or either fixture pair fail this check outright`.
- **Reject B — nondeterministic whole-package metadata.** Observed result: `rejected`. Two identical `.mlpackage` conversions whose model and weight data match but whose whole-package digests differ because `Manifest.json` carries fresh UUIDs fail the invariance half. Deciding clause: `The invariance pair must compare equal`, reinforced by the requirement to name the effect-bearing signal or subartifact instead of accepting unrelated metadata drift.
- **Reject C — irrelevant changed axis.** Observed result: `rejected`. The reviewer-constructed hypothetical in which `coremldata.bin` changes because the source package changed while `--optimize` affects `weights.bin` cannot establish the option's effect. Deciding clauses: `the changed-axis pair differs only in the input or option whose effect the step exists to detect` and `Name the effect-bearing signal or subartifact the comparison measures`.
- **Reject D — metadata- or receipt-only difference.** Observed result: `rejected`. A changed option that alters metadata or a receipt while the named effect-bearing signal remains equal does not satisfy discrimination. Deciding clause: `A difference confined to metadata, a receipt, or another output unrelated to the specified effect does not satisfy the changed-axis result`.
- **Guard reject — both fixtures accepted.** Observed result: `rejected`. A guard that accepts both the must-pass fixture and the minimally changed must-fail fixture is refused. Deciding clause: `a guard that accepts both is not a guard`.
- **Compliant case.** Observed result: `accepted`. Two same-kind pairs from the real pipeline were considered: identical inputs and configuration compare equal, while only the option under test changes in the second pair and the named effect-bearing signal compares different; the guard counterpart records one pass and one fail. Deciding clauses are the positive pair obligations, `The invariance pair must compare equal`, `the changed-axis pair must compare different in that named signal or subartifact`, and the explicit pass/fail guard requirement. No rejection clause fires.

## SC3 invariant attack

- **Reject A — narrowed declared set hides the third hypothesis.** Observed result: `rejected`. A three-hypothesis spec, a two-value declared output set, two branches, and no Deferred entry leaves the third known value uncovered. Deciding clauses: derive the set from `the union of the emitting step's declared output set and the origin spec's own enumeration` and require `every known value` to have its own next step.
- **Reject B — one representative does not cover the complement.** Observed result: `rejected`. For known set `{0,1}`, a special branch for resolved out-of-set value `2` does not cover produced value `3`. Deciding clauses: cover `any value outside the known set` as a category and `one representative out-of-set value does not cover the rest`.
- **Reject C — catch-all/unconsumed-verdict mutation.** Observed result: `rejected` for both subcases. Removing the catch-all rejection would admit one generic consumer; removing the unconsumed-verdict clause would let an emitter escape by having no consumer. Deciding clauses: `a single catch-all consumer that acts on "whatever the verdict says" covers nothing` and `a verdict no unit consumes is itself either a gap or a deliberate Deferred entry`.
- **Compliant case.** Observed result: `accepted`. Every value in the declared/spec union has a value-specific branch or a reasoned Deferred entry, unresolved measurement has a category-specific next step, and any resolved value outside the known set has a category-specific next step. Those are exactly the three positive coverage obligations; no catch-all or unconsumed verdict remains.

## Lifecycle attack

- **Transition/matrix bijection.** Observed result: `accepted`. R1 claims only T5; R2 claims T1, T2, T3, T4, T6, and T7; T0 remains U3 preparation; all eight rows have one evidence owner and every transition ID is unique. Deciding clause: `Duplicate IDs, duplicate row claims, missing rows, or extra mappings block Ship before any transition runs.`
- **Same-session R1 owner.** Observed result: `accepted`. The R1 body binds owner and executor to `the release-loop orchestrating session while it is executing shipping Step 8` and forbids worker delegation; all 18 U4 records name the release-loop orchestrator for R2 and the shipping Step 8 orchestrator for R1. The shipping hook is eligible only when invoked by `release-loop`, and the release-loop hook requires the section owner before either family runs. This closes the release-loop → shipping → R1 same-session handoff rather than treating relayed approval as execution authority.
- **Identity.** Observed result: `accepted`. The T5 boundary run accepted the canonical linked `main`/feature pair with an equal Git common directory and rejected the feature checkout as base, a nested base path, a foreign repository, and the wrong branch, each with exit 1 and the source/external sentinel retained. Deciding clauses are the canonical top-level, branch, unequal-checkout, and equal-common-directory tests in R1 step 1.
- **Collision.** Observed result: `accepted`. A mismatched identity owner produced exit 1 without changing archive/source state. Deciding clause: `assert all(candidate == owner for candidate in owner_candidates)` together with the identity-derived owner name.
- **Symlink.** Observed result: `accepted`. Destination, `.handoff/`, archive, stage, backup, and live-child symlink fixtures all exited 1 and retained the external sentinel/source. Deciding clauses are the `is_dir() and not ...is_symlink()` assertions and the manifest's per-entry `assert not path.is_symlink()`.
- **Marker seam.** Observed result: `accepted`. Cancellation at owner-only, operation-created, post-authoritative, and mid-install seams retained owned state; a rerun removed only the matching operation/owner and converged once. Deciding clauses create the identity owner before the operation, remove the operation before the owner, and recognize an already-authoritative destination before cleanup.
- **Mid-install.** Observed result: `accepted`. `t5-mid-install-before-progress` failed before the progress commit point, blocked cleanup, retained the complete backup record, and converged on a no-injection rerun; the two-crash restore case ran `1 → 1 → 0` while preserving the prior-base manifest. Deciding clauses install `progress.md` last, validate `backup-manifest.json` before restore, and require exact backup/destination manifests. The exact reachable backup-removal seam was also attacked from a valid R1 entry whose source and initial destination carry byte-recorded equal `{feature, spec, plan}` identities. The four invocations record packet SHA-256 and exit status as approved `c7569b7a7c0b8d0c04947eaff14090979361403bad8fa500a29f0e15e2cbdde4` / `1`, fixture-instrumented `bcd840e2fcedeee62162e9939b8bf9b203cef15a44f5e51a52aff4d74d4881d0` / `1`, approved `c7569b7a7c0b8d0c04947eaff14090979361403bad8fa500a29f0e15e2cbdde4` / `0`, and approved `c7569b7a7c0b8d0c04947eaff14090979361403bad8fa500a29f0e15e2cbdde4` / `0`; source and archive remained preserved and owned residuals converged. Decoding the fixture's recorded one-hunk diff reproduces the approved packet byte-for-byte on its old side and the recorded fixture hash on its new side; its only behavioral additions are the allowlist entry and an injected exit immediately after the line-855 verified rollback backup removal and before `backup_manifest_record.unlink()`. The deciding clauses are the initial same-identity entry assertion, backup-manifest equality before restore, restored-destination equality before backup removal, and manifest-only recovery branch; the older different-identity synthesized pre-state is correctly rejected at initial entry and is not a reachable counterexample.
- **Archive.** Observed result: `accepted`. The observed before/after archive manifests were byte-identical, including the nested empty directory and sentinel file, across success and recovery. Deciding clause: `assert manifest(destination / "archive", include_archive=True) == archive_before` before and after the progress commit.
- **Handoff/resume-order.** Observed result: `accepted`. The T5 trace was `merged-result-verified -> R1-start -> R1-accepted -> worktree-remove`; interrupted runs remained before cleanup, and the R2 success trace began only after the merged-clean shipping exit and R1 acceptance. The evidence deliberately did not remove a real host worktree; it exercised the isolated R1 program and recorded the surrounding procedural boundary. Deciding clauses are shipping's fixed `merge -> verify -> transition -> remove -> delete` order and release-loop's rule that merged authoritative state never re-enters pre-merge `shipping`, finishes pending cleanup after R1 acceptance, and then runs incomplete post-Ship transitions before Retro.
- **R2 first-hand gate.** Observed result: `accepted`. Direct approval recorded one current USER/orchestrator event with exact repository, issues, packet hash, and four mutations before extraction. Decline, deferral, relayed approval, and headless cases performed zero extraction/mutations and wrote blocked Ship state. Deciding clause: `only the human or orchestrating session receiving first-hand approval executes it`, with all other gate outcomes blocked.
- **R2 blocked recovery.** Observed result: `accepted`. A failure after durable approval but before extraction retained audit evidence but did not authorize retry; rerun rejected the old event and required a fresh gate. T7 read and post-write failures recorded landed/missing transitions, and the fresh-approved rerun skipped already-landed comments/closes without duplicates. Deciding clauses are R2 step 4's fresh read-only reconciliation, blocked-state write, and `Require fresh point-of-risk confirmation before rerunning the same packet`.
- **R2 terminal/Phase-6 gate.** Observed result: `accepted`. T7 success independently re-read exact bodies/states, wrote the SC6 completion line, cleared `blocked_reason`, and reached Phase 6 only after `T7-verified -> SC6-complete`; cancellation, failure, rollback, and headless outcomes remained in Ship. Deciding clauses require each post-Ship transition's acceptance evidence `before Phase 6` and state that a missing, failed, cancelled, or unverifiable transition blocks Ship.
- **All 48 runtime records.** Observed result: `accepted`. T0 success/forced-failure/rerun/rollback/headless/cancellation established one-time preparation without outward authority. T1–T4 carried the same six outcomes through a `gh-stub-only` boundary: read failure made zero mutations, post-write failure left the exact partial state, rerun skipped landed transitions, headless/cancellation required fresh approval, and rollback remained separately authorized. T5–T7 carried the six outcomes summarized above through `local-copy-only` or `gh-stub-only` boundaries, with real host/token variables unset and no outward endpoint reachable. Deciding clauses are the packet's read-before-write cases, R1's boundary-sentinel restriction on failure injection, and R2's first-hand/fresh-approval rules.

## Findings

No actionable P0–P2 finding survives. The only bounded observation is non-actionable: T5 intentionally does not execute real host worktree removal; it proves R1 inside disposable linked worktrees and the surrounding order through the shipped transition contracts and dispatch trace. That keeps removal ownership in existing `shipping` while still making R1 failure block cleanup. The record/package hashes, all 48 runtime records, all rejection/acceptance attacks, and all lifecycle boundary cases are internally consistent with the final U1–U4 bytes.

## Final verdict

clean
